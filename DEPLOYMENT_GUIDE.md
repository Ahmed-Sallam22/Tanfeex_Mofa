# 🚀 خطوات رفع المشروع على السيرفر (Production Deployment)

## ✅ الخطوات المطلوبة قبل الرفع:

### 1. مسح الكاش تماماً
```bash
# طريقة 1: استخدام السكريبت
./clear-cache.sh

# طريقة 2: يدوياً
rm -rf dist node_modules/.vite .vite
```

### 2. البناء للإنتاج
```bash
npm run build
```

### 3. التأكد من الملفات
```bash
ls -la dist/
```

يجب أن ترى:
- ✅ `index.html`
- ✅ `assets/` (مجلد JS و CSS)
- ✅ `_headers` (إعدادات الكاش)
- ✅ `service-worker.js` (لإدارة الكاش)

## 📤 رفع الملفات على السيرفر

### Option 1: باستخدام FTP/SFTP
1. افتح FileZilla أو WinSCP
2. اتصل بالسيرفر
3. احذف مجلد `dist` القديم على السيرفر
4. ارفع مجلد `dist` الجديد كاملاً

### Option 2: باستخدام SCP (Command Line)
```bash
# احذف المجلد القديم على السيرفر أولاً
ssh user@server "rm -rf /path/to/project/dist"

# ارفع المجلد الجديد
scp -r dist/* user@server:/path/to/project/dist/
```

### Option 3: باستخدام rsync (الأفضل)
```bash
rsync -avz --delete dist/ user@server:/path/to/project/dist/
```

## 🔄 بعد الرفع على السيرفر

### 1. مسح كاش Nginx/Apache
```bash
# إذا كنت تستخدم Nginx
sudo nginx -s reload

# إذا كنت تستخدم Apache
sudo systemctl reload apache2
```

### 2. مسح كاش CDN

#### Cloudflare:
1. ادخل على Dashboard
2. اختر موقعك
3. اذهب إلى "Caching"
4. اضغط "Purge Everything"

#### Fastly/CloudFront:
- استخدم لوحة التحكم الخاصة بهم لمسح الكاش

### 3. التحقق من التحديث
```bash
# تحقق من آخر تعديل للملفات
ssh user@server
cd /path/to/project/dist
ls -la
```

## 🌐 للمستخدمين النهائيين

أرسل رسالة للمستخدمين:

```
تم تحديث النظام! 🎉

للحصول على آخر التحديثات:
1. اضغط Ctrl + Shift + R (Windows)
   أو Cmd + Shift + R (Mac)

2. أو امسح الكاش من متصفحك:
   - Chrome: Settings > Privacy > Clear browsing data
   - Firefox: Options > Privacy > Clear History
   - Safari: Safari > Clear History
```

## 🔍 استكشاف المشاكل

### المستخدمون يرون النسخة القديمة؟

1. **تأكد من رفع الملفات الجديدة:**
```bash
ssh user@server
cat /path/to/project/dist/index.html | head -20
```

2. **تحقق من headers:**
```bash
curl -I https://your-domain.com
```
يجب أن ترى:
```
Cache-Control: no-cache, no-store, must-revalidate
```

3. **امسح كاش الـ Browser:**
- افتح Developer Tools (F12)
- اذهب لـ Network tab
- فعّل "Disable cache"
- أعد تحميل الصفحة

### الملفات لا تتحدث؟

```bash
# تأكد من الصلاحيات
ssh user@server
chmod -R 755 /path/to/project/dist
```

## 📋 Checklist قبل الرفع:

- [ ] مسح كل الكاش المحلي
- [ ] بناء نظيف بدون errors
- [ ] التأكد من ملف `_headers` موجود في dist
- [ ] اختبار محلي على `npm run preview`
- [ ] backup للنسخة القديمة على السيرفر
- [ ] رفع الملفات الجديدة
- [ ] مسح كاش السيرفر
- [ ] مسح كاش CDN
- [ ] اختبار من متصفح نظيف (Incognito mode)
- [ ] إخبار المستخدمين بالتحديث

## 💡 نصائح مهمة:

1. **دائماً احتفظ بنسخة احتياطية:**
```bash
ssh user@server
cd /path/to/project
cp -r dist dist.backup.$(date +%Y%m%d)
```

2. **استخدم versioning في اسم الملفات:**
   - Vite يقوم بذلك تلقائياً بإضافة hash للملفات

3. **راقب logs السيرفر:**
```bash
tail -f /var/log/nginx/error.log
```

4. **اختبر من أجهزة مختلفة:**
   - Desktop
   - Mobile
   - Tablet
   - متصفحات مختلفة

## 🎯 الخلاصة:

```bash
# 1. مسح الكاش
./clear-cache.sh

# 2. البناء
npm run build

# 3. الرفع
rsync -avz --delete dist/ user@server:/path/to/project/dist/

# 4. إعادة تشغيل السيرفر
ssh user@server "sudo nginx -s reload"

# 5. مسح كاش CDN من لوحة التحكم

# ✅ تم!
```
